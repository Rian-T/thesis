%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\chapter{Modern AI and New Opportunities}
\label{chap:modern-ai}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% =====================================================================
% TODO — chapter to be written (scribe pass, voice-first). Scaffold only.
% Scope note: ch.1 already covers the history (expert systems -> ML ->
% scaling era), and ch.3 covers what biomedical text is. This chapter is
% only: what contemporary AI makes newly possible (OncoLab) and the wall
% it hits (no shareable clinical data), as the bridge to ch.3.
% =====================================================================

\paragraph{A concrete opportunity: the OncoLab project.}
\begin{itemize}
  \item TODO: describe OncoLab — goal, clinical setting, the information-extraction
        task (oncology reports / structuring).
  \item TODO: what today's models make newly feasible here, and the practical stakes
        (cohort selection, report structuring, decision support).
\end{itemize}

\paragraph{The challenge: opportunity without data.}
\begin{itemize}
  \item TODO: the wall — clinical text is personal data, not shareable; no large
        real French clinical corpus to train or even evaluate on.
  \item TODO: so the most direct lever, fine-tuning on in-domain clinical data, is
        unavailable; we must build from public text instead.
  \item TODO: this is the bridge to the next chapter — to use public text for the
        clinic we first need to know what biomedical text is.
\end{itemize}

% =====================================================================
% MIGRATED RAW MATERIAL (from the old ch.1 intro) — to fold into ch.2 prose.
% P6 below = the "risk / data wall" material, belongs in this chapter.
% =====================================================================
% However, healthcare faces a limitation that other domains do not: the
% confidentiality and scarcity of annotated clinical data. Medical data is
% sensitive and difficult to obtain in large quantities for model training. Works
% adapting language models to the medical domain often rely on public data such as
% PubMed or patient forums, but these do not always reflect the real clinical
% language used in electronic health records. Moreover, while MIMIC exists in the
% United States, public clinical datasets remain scarce in other countries such as
% France.
%
% NOTE — the old P5 (UMLS/SNOMED/RxNorm knowledge bases vs. data) is NOT ch.2
% material: it is Related Works / OntoBook territory. Parked here only so it is not
% lost; do not put it in ch.2 prose:
% Despite healthcare offering many structured and curated knowledge bases such as
% UMLS, SNOMED-CT, and RxNorm, general-purpose models pretrained on massive
% unstructured data outperform approaches based on domain-specific rules and
% knowledge bases.
