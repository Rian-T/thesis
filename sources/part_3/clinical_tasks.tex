\partepigraph{``the speaking of language is part of an activity, or of a form of
life''}{%
Ludwig Wittgenstein, \textit{Philosophical Investigations}}

The first two parts built a public biomedical corpus and pretrained encoders on
it. This part asks whether those public foundations can support a real clinical
task without any training on hospital data: reading a patient's free-text
reports and filling the structured form that a clinical study keeps for each
patient. The task is demanding because the same value plays different roles as
the disease unfolds, and because no real filled form is available to learn from.
Deployment sharpens the stakes. A hospital cannot move its records to a cloud
API, which confidentiality law forbids, and it cannot fit a multi-billion-parameter
generator on the hardware a clinical service runs. Accuracy alone is therefore not
enough: the model must also run on site, and only a small encoder has a footprint
that fits. This is the same privacy constraint that motivated the public corpus
and encoders of the first two parts. The next three chapters
build task supervision from public and generated text, design an extractor that
can be asked for new fields at inference time, and compare a small biomedical
encoder with much larger generative models. The result they build toward is that
a 150-million-parameter encoder, trained only on public and generated text and
never shown a real hospital record, nearly matches, on a synthetic version of the
task, the accuracy of a generative model about twenty-seven times its size
fine-tuned on the same data.

\chapter{Synthetic Data for Task Adaptation}
\label{chap:synthetic}
\input{sources/part_3/chapter9/article}

\chapter{Architectures for Low-Resource Extraction}
\label{chap:architectures}
\input{sources/part_3/chapter8/article}

\chapter{Evaluating Open-Vocabulary Extraction}
\label{chap:evaluation}
\input{sources/part_3/chapter7/article}
