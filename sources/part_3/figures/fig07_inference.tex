\begin{figure}[t]
  \centering
  \resizebox{0.9\textwidth}{!}{%
  \begin{tikzpicture}[font=\small\sffamily,
    tok/.style={font=\small\sffamily, text=ThesisInk},
    span/.style={rounded corners=2pt, draw=ThesisPrimaryDark, line width=0.9pt,
      fill=ThesisPrimary!24, inner sep=3pt, font=\small\sffamily\bfseries,
      text=ThesisInk},
    query/.style={rounded corners=3pt, draw=ThesisNeutral!55, line width=0.9pt,
      fill=ThesisNeutral!12, inner sep=6pt, align=center, font=\small\sffamily,
      text=ThesisInk},
    sim/.style={circle, draw=ThesisInk!70, line width=0.9pt, fill=ThesisPaper,
      inner sep=0pt, minimum size=2.15cm, align=center, font=\small\sffamily},
    ans/.style={rounded corners=3pt, draw=ThesisSecondaryDark, line width=1pt,
      fill=ThesisSecondary!30, inner sep=6pt, font=\small\sffamily\bfseries,
      text=ThesisInk},
    pull/.style={-{Stealth[length=2.4mm]}, line width=0.9pt,
      shorten >=3pt, shorten <=3pt},
  ]

    % --- Clinical text (top), a line of tokens with one candidate span boxed ---
    \node[tok] (t0) at (0,2.5) {\dots{} début chimiothérapie par protocole};
    \node[span, right=3pt of t0] (cand) {CHOEP-14};
    \node[tok, right=3pt of cand] (t1) {, 8 cycles \dots};
    \node[text=ThesisNeutral, font=\scriptsize\sffamily, anchor=west]
      at ([yshift=13pt]t0.west) {\textbf{Clinical text}};

    % --- Type description (bottom), the open query ---
    \node[query] (desc) at (1.05,-1.9) {\textbf{Type description}\\[2pt]
      ``first-line treatment''};

    % --- Similarity node (centre-right), spaced from both inputs ---
    \node[sim] (sim) at (6.15,0.3) {span--\\description\\similarity};

    % --- Answer (right) ---
    \node[ans] (ans) at (10.0,0.3) {CHOEP-14};

    % --- Arrows: both inputs land on mirror-image points of the circle and the
    %     answer exits right; all three are faded flow arrows, so the role colours
    %     live on the nodes (lavender span, lime answer), not on the arrows ---
    \draw[pull, ThesisNeutral!55] (cand.south east)
      to[out=-28,in=152] (sim.north west);
    \draw[pull, ThesisNeutral!55] (desc.north east)
      to[out=28,in=-152] (sim.south west);
    \draw[pull, ThesisNeutral!55] (sim.east) -- (ans.west);

  \end{tikzpicture}%
  }
  \caption{Span selection from a clinical text and a type description.}
  \label{fig:p3-open-query}
\end{figure}
