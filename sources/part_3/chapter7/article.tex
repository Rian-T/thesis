\section{Introduction}

The extractor of the previous chapter can be asked for a type it never saw. That is the point of it, and it is also what makes it hard to score. The usual way to measure extraction compares a model's output against a fixed set of human annotations. But those annotations cover only the types the annotators were told to mark, under one set of conventions about where a span begins and ends and what counts as an entity. A model rewarded for matching those conventions is not necessarily better at finding what a new study cares about; it may have learned the house style of one annotation effort. To see whether our model generalises, we need an evaluation that does not assume a fixed answer key.

\section{A Reference-Free Evaluation with a Language-Model Judge}

We evaluate without a gold standard. We take 400 clinical case documents the models were never trained on, and a bank of twenty types: ten common clinical types, such as disease, drug, and symptom, and ten novel types that no benchmark uses, built to be compositional or cross-domain, such as an adverse effect, an implantable device, or an environmental risk factor. Each model runs twice, once for the common types and once for the novel ones, so the two can be measured apart. We pool the spans every model proposes, add candidates of the judge's own, remove duplicates, and hide which system produced which span. A large language model then judges each span against its type description and labels it correct, wrong type, wrong boundary, or not an entity. We score with mean average precision, which does not depend on a decision threshold, and we summarise generalisation by the gap between a model's score on common types and its score on novel ones. \Cref{fig:judge-schema} sets out the whole procedure.

To keep the judge honest we seed the pool with controls: real clinical entities that should be accepted, and function words mislabelled as diseases that should be rejected. We keep only the runs where the judge handles these correctly.

\begin{figure}[t]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    font=\small\sffamily,
    >=Stealth,
    inbox/.style={pipe box, draw=ThesisSecondaryDark, fill=ThesisSecondary!15,
               rounded corners=2pt, line width=0.7pt, align=center,
               inner sep=5pt, minimum width=2.6cm, minimum height=0.78cm, text=ThesisInk},
    proc/.style={pipe box, draw=ThesisTertiaryDark, fill=ThesisTertiary!15,
                 rounded corners=2pt, line width=0.7pt, align=center,
                 inner sep=6pt, minimum width=2.4cm, minimum height=1.2cm, text=ThesisInk},
    judge/.style={pipe box, draw=ThesisPrimaryDark, fill=ThesisPrimary!18,
                  rounded corners=3pt, line width=1.1pt, align=center,
                  inner sep=7pt, minimum width=2.4cm, minimum height=1.4cm, text=ThesisInk},
    res/.style={pipe box, draw=ThesisNeutral, fill=ThesisPaper,
                rounded corners=2pt, line width=0.7pt, align=center,
                inner sep=4pt, minimum width=2.9cm, minimum height=0.56cm, text=ThesisInk},
    score/.style={pipe box, draw=ThesisPrimaryDark, fill=ThesisPrimary!10,
                  rounded corners=2pt, line width=0.8pt, align=center,
                  inner sep=5pt, minimum width=2.55cm, minimum height=1.0cm, text=ThesisInk},
    flow/.style={->, line width=0.9pt, ThesisInk!45, shorten >=2pt, shorten <=2pt},
    thin/.style={-, line width=0.7pt, ThesisInk!35},
    runlbl/.style={font=\scriptsize\itshape, ThesisInk!60}]

  \def\xin{0}
  \def\xpool{4.0}
  \def\xjudge{8.0}
  \def\xres{12.0}
  \def\xscore{15.4}

  % inputs
  \node[inbox] (preds) at (\xin, 1.05) {predicted span};
  \node[inbox] (type)  at (\xin,-0.55) {type description};

  % pooling
  \node[proc] (pool) at (\xpool, 0.25) {pooled\\de-duplicated\\blind};

  % judge (heart of the figure)
  \node[judge] (judge) at (\xjudge, 0.25) {language-model\\judge};

  % verdicts
  \node[res] (v1) at (\xres, 2.05) {correct};
  \node[res] (v2) at (\xres, 1.05) {wrong type};
  \node[res] (v3) at (\xres, 0.05) {wrong boundary};
  \node[res] (v4) at (\xres,-0.95) {not an entity};

  % scores
  \node[score] (map) at (\xscore, 0.55) {mean average\\precision};
  \node[score, draw=ThesisTertiaryDark, fill=ThesisTertiary!12]
        (gap) at (\xscore,-1.15) {overfit gap\\\scriptsize common $-$ novel};

  % inputs -> pool
  \draw[flow] (preds.east) -- ([yshift=4pt]pool.west);
  \draw[flow] (type.east)  -- ([yshift=-4pt]pool.west);
  % pool -> judge
  \draw[flow] (pool.east) -- (judge.west);
  % judge -> verdicts
  \coordinate (hub) at (judge.east);
  \draw[flow] (hub) -- (v1.west);
  \draw[flow] (hub) -- (v2.west);
  \draw[flow] (hub) -- (v3.west);
  \draw[flow] (hub) -- (v4.west);
  % verdicts collected on a bus, then one arrow to the score
  \coordinate (rail) at ($(\xres+1.6,0.55)$);
  \draw[thin] (v1.east) -- (v1.east -| rail);
  \draw[thin] (v2.east) -- (v2.east -| rail);
  \draw[thin] (v3.east) -- (v3.east -| rail);
  \draw[thin] (v4.east) -- (v4.east -| rail);
  \draw[thin] (v1.east -| rail) -- (v4.east -| rail);
  \draw[flow] (rail) -- (map.west);
  % score -> gap (taken across the two runs)
  \draw[flow] (map.south) -- (gap.north);

  % the judge step is run twice (common / novel types)
  \node[runlbl, align=center] at ($(\xpool,-1.15)!0.5!(\xjudge,-1.15)$)
        {run twice:\\common $\cdot$ novel types};

\end{tikzpicture}}
\caption{The reference-free judge. Each predicted span is paired with its type description; the spans from all systems are pooled, de-duplicated, and stripped of their provenance, then a blind language-model judge labels each one correct, wrong type, wrong boundary, or not an entity, run once for common types and once for novel ones, and summarised by mean average precision and the common-minus-novel overfit gap.}
\label{fig:judge-schema}
\end{figure}

\section{Benchmark Winners Do Not Generalise}

On the common types the models are close. The novel types separate them, as \Cref{tab:judge-map} shows. Our entity-tuned configuration reaches a mean average precision of $0.476$ on novel types, the best of any model we test. The baselines collapse: a strong single-encoder model tuned to a standard benchmark scores well on common types but never makes a confident prediction on a novel one, and a published biomedical extractor falls to $0.121$ on novel types. The gap between common and novel performance, which measures how far a model has overfit to familiar types, is small for our models and large for the baselines. The entity-tuned model is ahead of the convention-tuned baseline on novel types by $+0.095$ in mean average precision, with a $95\%$ bootstrap confidence interval of $[+0.063, +0.131]$.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{10pt}
\sisetup{detect-weight=true, table-format=1.3}
\begin{tabular}{l S S S[table-format=+1.3]}
\toprule
\textbf{Model} & {\textbf{MAP common}} & {\textbf{MAP novel}} & {$\boldsymbol{\Delta}$\textbf{overfit}} \\
\midrule
\tablegroup{4}{Our models}
Entity-tuned & 0.583 & \bfseries 0.476 & +0.107 \\
Multi-task & 0.534 & 0.302 & +0.232 \\
\midrule
\tablegroup{4}{Baselines}
Convention-tuned baseline & 0.569 & 0.381 & +0.188 \\
GLiNER2-large             & 0.496 & 0.285 & +0.211 \\
GLiNER-biomed             & 0.390 & 0.121 & +0.269 \\
\bottomrule
\end{tabular}
\caption{Reference-free judge scores on common and novel types: the entity-tuned model has the best novel-type mean average precision and the smallest common-to-novel gap, while the baselines drop sharply on novel types.}
\label{tab:judge-map}
\end{table}

Ranked on a standard benchmark, the same models come out in almost the opposite order (\Cref{fig:benchmark-vs-judge}): the convention-tuned baseline first, our multi-task configuration last. A model can top the benchmark and still be the weakest at finding new types, because the benchmark measures agreement with one annotation convention rather than the ability to generalise. A lower benchmark score is what we should expect from a model that has not overfit to a single convention, and it is the entity-tuned configuration, weaker on the benchmark, that generalises best.

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{plots/chapter18/benchmark_vs_judge.pdf}
\caption{The same models ranked two ways. On the standard benchmark (left, strict
macro-$F_1$) the convention-tuned baseline ranks first and the multi-task
configuration last. On generalisation to novel types (right, reference-free judge
MAP) the order is almost reversed, and the entity-tuned configuration, weaker on
the benchmark, generalises best. Each model keeps its colour across the panels.}
\label{fig:benchmark-vs-judge}
\end{figure}

\section{Is the Judge Trustworthy?}

Using a language model to grade extractions invites two worries: that the judge is unreliable, and that it favours models like itself. We check both. Against human annotations on clinical types where the gold is unambiguous, the judge agrees at a Cohen's kappa of $0.532$, rising to $0.797$ on the cleanest set (\Cref{tab:judge-validity}). Two judges from different model families agree with each other at $0.728$, so the result does not rest on a single judge.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{10pt}
\begin{tabular}{l S[table-format=1.3]}
\toprule
\textbf{Annotated set} & {\textbf{Cohen's }$\boldsymbol{\kappa}$} \\
\midrule
CAS1    & 0.257 \\
CAS2    & 0.544 \\
E3C     & 0.797 \\
EMEA    & 0.560 \\
MEDLINE & 0.502 \\
\midrule
Global (judge vs human gold) & 0.532 \\
Inter-judge (Qwen vs MedGemma) & 0.728 \\
\bottomrule
\end{tabular}
\caption{The judge agrees with human gold at a Cohen's kappa of $0.532$ overall and $0.797$ on the cleanest set, and the two judges agree with each other at $0.728$.}
\label{tab:judge-validity}
\end{table} Re-scoring everything with a second, independent medical model leaves the rankings in place and slightly sharper, which rules out self-preference. Where the judge and the human gold disagree, $85\%$ of the disagreements are cases where the human annotation followed a convention the judge reasonably declined to follow, not cases where the judge was wrong.

What a convention looks like is easiest to see in the disagreements (\Cref{fig:judge-disagreements}). The word \textit{hépatique} (hepatic) is marked as an anatomical entity because the guideline projects a relational adjective onto the organ it modifies. The participle \textit{traités} (treated) is marked as a procedure because the guideline extends the type to inflected forms of a treatment. The phrase \textit{sans particularité} (unremarkable) is marked as an examination value. Given a plain description of each type, the judge rejects all three, since the word is not an organ, an act, or a measurement. In every case the judge is right about what the type means, and the gold is following a labeling convention.

\begin{figure}[t]
\centering
% displaCy-style: whole passage, gold spans in lavender with type tag above;
% solid border = judge agrees, dashed peach + verdict below = judge refuses the type.
\providecommand{\gtafull}[1]{\raisebox{0.15ex}{\scriptsize\textsc{\textcolor{ThesisPrimaryDark}{#1}}}}
\providecommand{\jtafull}[1]{\scriptsize\textsc{\textcolor{ThesisTertiaryDark}{#1}}}
\begin{tikzpicture}[font=\normalsize\sffamily, text=ThesisInk,
  ok/.style ={fill=ThesisPrimary!28, draw=ThesisPrimaryDark, rounded corners=3pt,
              line width=0.6pt, inner xsep=4pt, inner ysep=2.5pt},
  bad/.style={fill=ThesisPrimary!28, draw=ThesisTertiaryDark, dashed, rounded corners=3pt,
              line width=0.8pt, inner xsep=4pt, inner ysep=2.5pt},
  gtag/.style={anchor=base, font=\scriptsize\sffamily},
  jtag/.style={fill=ThesisTertiary!22, draw=ThesisTertiaryDark, rounded corners=2.5pt,
               line width=0.5pt, inner xsep=3.5pt, inner ysep=2pt, font=\scriptsize\sffamily},
  drop/.style={-{Stealth[length=1.7mm]}, ThesisTertiaryDark!70, line width=0.6pt,
               shorten >=1pt, shorten <=1pt}]
  \def\rh{2.35}
  % row 1
  \node[anchor=base west] (a) at (0,0) {Patient suivi pour un };
  \node[ok, anchor=base west] (lymphome) at (a.base east) {lymphome};
  \node[anchor=base west] (b) at (lymphome.base east) {, };
  \node[bad, anchor=base west] (traite) at (b.base east) {traité};
  \node[anchor=base west] (c) at (traite.base east) { par };
  \node[ok, anchor=base west] (rchop) at (c.base east) {R-CHOP};
  \node[anchor=base west] (d) at (rchop.base east) {.};
  \node[gtag, anchor=base] at ([yshift=4.5mm]lymphome.north) {\gtafull{disease}};
  \node[gtag, anchor=base] at ([yshift=4.5mm]rchop.north)   {\gtafull{drug}};
  \node[gtag, anchor=base] at ([yshift=4.5mm]traite.north)  {\gtafull{procedure}};
  \node[jtag, anchor=north] (jt1) at ([yshift=-5.5mm]traite.south) {\jtafull{wrong type}};
  \draw[drop] (traite.south) -- (jt1.north);
  % row 2
  \node[anchor=base west] (e) at (0,-\rh) {La fonction };
  \node[bad, anchor=base west] (hepa) at (e.base east) {hépatique};
  \node[anchor=base west] (f) at (hepa.base east) { est normale et l'};
  \node[ok, anchor=base west] (exam) at (f.base east) {examen clinique};
  \node[anchor=base west] (g) at (exam.base east) { est };
  \node[bad, anchor=base west] (sp) at (g.base east) {sans particularité};
  \node[anchor=base west] (h) at (sp.base east) {.};
  \node[gtag, anchor=base] at ([yshift=4.5mm]hepa.north) {\gtafull{anatomy}};
  \node[gtag, anchor=base] at ([yshift=4.5mm]exam.north) {\gtafull{exam}};
  \node[gtag, anchor=base] at ([yshift=4.5mm]sp.north)   {\gtafull{value}};
  \node[jtag, anchor=north] (jt2) at ([yshift=-5.5mm]hepa.south) {\jtafull{wrong type}};
  \draw[drop] (hepa.south) -- (jt2.north);
  \node[jtag, anchor=north] (jt3) at ([yshift=-5.5mm]sp.south) {\jtafull{not an entity}};
  \draw[drop] (sp.south) -- (jt3.north);
  % legend
  \coordinate (lx) at (0,-2*\rh-0.2);
  \node[ok, anchor=west, minimum width=5mm, minimum height=3.5mm] (lg1) at (lx) {};
  \node[anchor=west, font=\scriptsize] at (lg1.east) {gold span, judge agrees};
  \node[bad, anchor=west, minimum width=5mm, minimum height=3.5mm] (lg2) at ([xshift=5.2cm]lx) {};
  \node[anchor=west, font=\scriptsize] at (lg2.east) {gold span, judge refuses the type};
\end{tikzpicture}
\caption{The same clinical passage under two annotation layers: most gold spans
match the judge, and the three it refuses (a participle, a relational adjective,
a qualitative negation) diverge on labeling convention rather than fact.}
\label{fig:judge-disagreements}
\end{figure}

The papers behind these gold sets already grant that their labels are conventional. The authors of QUAERO state that there is no consensus on what counts as an entity and that their scheme is a deliberate choice~\citep{neveol14quaero}. The DEFT~2020 campaign supplies the clinical-entity labels we use over the case reports of CAS~\citep{grabar_cas_2018}. Its two human annotators agreed at only $0.67$ in strict scoring, and at $0.80$ once boundaries were allowed to differ slightly~\citep{cardon_presentation_2020}. They report diseases and signs as the hardest categories to annotate consistently, down to spans where one annotator wrote a word and another a synonym and the two counted as a disagreement. The E3C corpus goes one step further and separates a fully manual gold layer from a mostly automatic silver one~\citep{magnini_e3c_2021}. Each of these grants that the gold is conventional, yet none measures how much of a model's apparent error is convention rather than mistake. The reference-free judge supplies that measurement, and most of the error is convention.

This low ceiling on human agreement is the reason a benchmark cannot do the work in its place. When two trained annotators agree only $0.67$ of the time in strict scoring, exact match against either of them is bounded well below a model's real ability, and a model can be right about the medicine and still be scored as wrong. It also explains why the judge agrees with the gold most where the gold is cleanest: a kappa of $0.797$ on the carefully annotated E3C set against $0.257$ on the noisiest one. Agreement rises with the quality of the gold.

The judge is conservative: it under-credits correct spans, so the absolute scores understate real performance while the rankings between models stay stable. One limit remains. The human check covers only the common types, so the headline claim about novel types has no direct human anchor yet, and a small human study of novel-type judgements is the most useful next step.

\section{The OncoLab Lymphoma Cohort}

The last test applies the whole thesis to a real clinical problem. The OncoLab cohort asks for $89$ fields of a lymphoma annotation form, and we evaluate on the held-out synthetic dossiers built in \Cref{chap:synthetic}, scoring each field by how well the model locates its value in the text. Every earlier part of the thesis meets here: the public corpora that supplied the seeds (\Cref{chap:collecting,chap:quality,chap:mcbio}), the modern French biomedical encoder (\Cref{chap:objectives,chap:ontobook}), the open-vocabulary architecture (\Cref{chap:architectures}), and the synthetic dossiers themselves (\Cref{chap:synthetic}).

Off the shelf, no model does well. The best zero-shot result is a micro-averaged F-score of $0.174$, and most models sit between $0.10$ and $0.15$. The convention-tuned baseline is precise but barely fires, finding only a small fraction of the fields. The task is long and spread across several documents, and reading it cold is beyond every model we try.

After fine-tuning on the synthetic dossiers, the picture changes (\Cref{tab:lymphoma-capstone}). Every model rises to about $0.31$ to $0.33$, and the differences between them nearly vanish. The synthetic data closes most of the distance, and it closes it for all of them: the benchmark champion and the generalist extractor reach the same ceiling. The scores are still low in absolute terms, which measures how hard the task is rather than showing it solved. The result we set out to reach holds here: built only from public and synthetic data, a small open model reaches the same level on a real oncology task as a model tuned on a standard benchmark, without ever seeing a real clinical note.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{12pt}
\sisetup{detect-weight=true, table-format=1.3}
\begin{tabular}{l S S}
\toprule
\textbf{Model} & {\textbf{Zero-shot}} & {\textbf{After fine-tuning}} \\
\midrule
GLiNER-biomed             & \bfseries 0.174 & \bfseries 0.329 \\
Entity-tuned              & 0.150 & 0.318 \\
Convention-tuned baseline & 0.138 & 0.324 \\
Multi-task                & 0.106 & 0.319 \\
\bottomrule
\end{tabular}
\caption{Micro-F1 on the OncoLab lymphoma cohort: zero-shot the models are low and spread out, but after fine-tuning on the synthetic dossiers they all converge to about $0.32$.}
\label{tab:lymphoma-capstone}
\end{table}

\section*{Conclusion}

The parts of the thesis come together on one cohort. A public biomedical corpus, an encoder trained on it, a training objective with a dose of structured knowledge, an open-vocabulary architecture, and data that is generated rather than collected all meet on one relapsed-lymphoma cohort and let a model that never saw a real clinical note reach a usable level of extraction. The conclusion of the thesis steps back from these numbers to ask what they say about building clinical models from public data, and where the approach still falls short.
